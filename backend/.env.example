# ──────────────────────────── Secrets ────────────────────────────
# These MUST be set and kept out of version control.

# Database
DATABASE_URL=postgresql://kenaz:kenaz@db:5432/kenaz

# Google OAuth (get from Google Cloud Console)
GOOGLE_CLIENT_ID=your-google-client-id-here
GOOGLE_CLIENT_SECRET=your-google-client-secret-here

# JWT Secret (generate with: openssl rand -hex 32)
JWT_SECRET_KEY=dev-secret-key-change-in-production

# Payment gateway credentials (required if PAYMENT_GATEWAY_TYPE=tpay)
# TPAY_CLIENT_ID=your-tpay-client-id
# TPAY_CLIENT_SECRET=your-tpay-client-secret

# ──────────────────────────── Variables ──────────────────────────
# Non-secret configuration.

# Domain — FRONTEND_URL and GOOGLE_REDIRECT_URI are computed from these two.
# APP_SCHEME=https
# APP_HOST=yourdomain.com

# Override computed URLs only if your setup differs from the defaults:
#   FRONTEND_URL = APP_SCHEME://APP_HOST
#   GOOGLE_REDIRECT_URI = APP_SCHEME://APP_HOST/auth/google/callback
#
# Production (nginx proxies /auth/google/callback → backend):
# GOOGLE_REDIRECT_URI=https://yourdomain.com/auth/google/callback
#
# Development (uvicorn only, no nginx — must include /api prefix):
# GOOGLE_REDIRECT_URI=http://localhost:8000/api/auth/google/callback
# FRONTEND_URL=https://yourdomain.com

# Payment gateway type: "fake" or "tpay"
# PAYMENT_GATEWAY_TYPE=fake

# Default payment link (shown in event creation form)
# DEFAULT_PAYMENT_URL=https://buycoffee.to/kenazplus

# Root admin email – first login with this email gets auto-admin
ROOT_ADMIN_EMAIL=your-admin@example.com

# Debug mode
# DEBUG=True
